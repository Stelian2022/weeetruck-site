{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<link rel="stylesheet" href="{{ asset('./assets/css/dashbord.css') }}">
 <div class="user"></div>   <h1>Employe {{ app.user.nom }}</h1>
 <div class="button">
			<a href="{{ path('employe') }}">Retour au tableau de bord</a>
		</div>
    <table class="table">
        <tbody>
            <tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
         
           
        </tbody>
    </table>

  
<div class="button">
    <a href="{{ path('app_user_crud_edit', {'id': user.id}) }}">Modifier</a>
</div>
    {# {{ include('user_crud/_delete_form.html.twig') }} #}
{% endblock %}
